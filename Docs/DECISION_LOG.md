# OPEN PLAN - Decision Log

## D001 - Reproducible authored scene generation

Office scenes, shared materials, imported assets, and settings are generated by the checked-in Unity editor pipeline. Runtime stage selection occurs inside the shared Office bootstrap scene.

## D002 - FBX contract

Blender generates low-poly foundational meshes. Unity consumes copied FBX exports, preserving importer axis conversion on visual children while gameplay components live on unscaled wrappers.

## D003 - Transform-driven characters

Workers retain their modular imported meshes and lightweight transform animation. Readability is prioritized over a skeletal facial rig.

## D004 - Deterministic simulation

Worker choices use a single seedable random service and bounded decision intervals. Stage selection is also deterministic and covered independently from simulation randomness.

## D005 - Original office preservation

The large `OfficeEnvironmentBuilder` is the Established Office implementation. It is extended only with placement-zone metadata; its art, amenities, desk layout, lighting, camera composition, worker simulation, hiring/firing systems, and release evidence remain intact.

## D006 - One bootstrap scene, stage-aware director

All stages use the existing Office scene and one `OfficeDirector`. The director selects an environment builder and a starting roster, then runs the same task, economy, workday, worker, hiring, firing, UI, audio, and camera initialization. This avoids duplicated gameplay systems and fragile scene variants.

## D007 - Starter first

The Main Menu explicitly selects `StarterOffice`. Direct scene loads also default to Starter. `EstablishedOffice` is available only through an explicit developer/automation selection until a later expansion milestone exposes it in normal play.

## D008 - Untimed starter, compatible established preview

Starter stages never auto-finish. Their progress is tied to revenue. The Established Office retains its legacy timed workday so the released management loop and automated evidence paths stay operational during the pivot.

## D009 - Scope boundary for checkpoint 1

The Starter Office is intentionally a temporary minimal environment at this checkpoint. Placement data and all six zone types exist, but player carrying, complete activity effects, and the physical purchase interaction remain assigned to their dedicated later checkpoints.
