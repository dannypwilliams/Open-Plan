# Asset Pipeline

See `Tools/Blender/README_OPEN_PLAN.md` for exact generation and validation commands. The generator is deterministic in layout and naming, exports individual low-poly FBX files, preserves compressed `.blend` sources, and creates JSON manifests. Unity never reads `.blend` files.

